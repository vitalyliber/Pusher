<div class="container mx-auto px-6 lg:px-0">
  <div class="max-w-5xl mx-auto bg-gray-50 py-10 rounded-sm">
    <% if mobile_access %>
    <% else %>
      <p class="text-center text-gray-500">
        Please log in to continue
      </p>
    <% end %>
  </div>
</div>
